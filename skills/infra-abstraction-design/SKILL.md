---
name: infra-abstraction-design
description: Design abstractions around infrastructure or platform capabilities.
---

## When to use
Use when repeated infra patterns need structure.

## Notes
- Align the work with the active `AGENTS.md`, `ARCHITECTURE.md`, and any active roles.
- Prefer existing commands in `./commands/` when a deterministic workflow exists.
