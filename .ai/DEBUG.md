# Debug History
- Generative-media validation failed on correct Markdown headings and style-index entries under WSL because Git-managed files contained CRLF and exact-line grep treated the trailing CR as content. Normalize CR before exact comparisons; commands/validate-media-templates.sh now normalizes the first heading and tests/test-generative-media.sh uses a CR-normalizing exact-line helper. Verified by the full generative-media and OpenCaw suites.
- commands/append-debug.sh failed before writing because Bash printf parsed the leading hyphen in its format string as an option. Use printf -- when a format string begins with '-'. Verified by successfully appending the generative-media CRLF resolution afterward.
- commands/append-rules.sh failed before writing because Bash printf parsed its leading-hyphen format as an option. Added printf -- and memory-system regression coverage; rerun the correction recorder successfully.
- commands/append-project-memory.sh could not replace a valid fact in a CRLF-managed memory file because its awk comparison retained the carriage return. Normalize CR before replacement matching; verified with a CRLF regression fixture.
- tests/test-generative-media.sh used tr piped to grep -q under pipefail for CRLF-normalized exact-line matching; a valid early grep match could SIGPIPE tr and fail the test. Replaced the pipeline with a single AWK comparison and reran the media suite.
- commands/validate-memory.sh treated CRLF blank lines as non-empty invalid tagged memory content and used an exact grep that could not match CRLF system defaults. Normalize trailing CR during validation and compare defaults with AWK; covered by the CRLF replacement fixture.
- commands/repo-map-status.sh did not parse or replace CRLF fingerprint markers; --stamp reported current while leaving the old marker unchanged. Strip trailing CR during marker parsing and stamping, with CRLF recognition and restamp regression coverage.
- commands/clean-context.sh failed to recognize CRLF Archived-on markers and would recompact completed tasks. Normalize trailing CR during archive-state detection and verify dry-run idempotence with a CRLF archived task fixture.
- commands/validate-memory.sh normalized the repository-map fingerprint through tr piped to grep under pipefail; an early match could SIGPIPE tr and intermittently report a valid marker as missing. Replaced the pipeline with one CRLF-safe AWK check and verified repeated memory validation.
